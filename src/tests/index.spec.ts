describe('index', () => {
/*     let frag : any;
    beforeEach(() => {
        frag = typeof $.fn.slider
    }) */
    it('should be defined', () => {
        expect($($.fn.slider)).toBeDefined();
    });
});
